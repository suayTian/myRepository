spring:
  cloud:
    config:
      profile: dev
      name: provider-server
      #label: master
      uri: http://localhost:9003
      #discovery: #要启用discover eureka配置需要在本地 spring cloud启动时会先初始化spring cloud组件
        #enabled: true
        #service-id: config-server
      username: admin
      password: 123456